import { observer } from 'mobx-react-lite';
import React, { FC } from 'react';

const SoglPageOk:FC = () => {
    return (
        <div>
            OK
        </div>
    );
};

export default observer(SoglPageOk);